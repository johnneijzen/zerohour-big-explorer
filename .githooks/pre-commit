#!/usr/bin/env bash
set -euo pipefail

# Run formatting and lint checks before commit
echo "Running pre-commit checks: rustfmt, clippy"

echo "Checking rustfmt..."
cargo fmt --all -- --check

echo "Running clippy..."
cargo clippy --workspace --all-targets -- -D warnings

echo "Pre-commit checks passed."
